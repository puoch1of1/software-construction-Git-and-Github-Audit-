diff --git a/disk_usage_original.py b/disk_usage_fixed.py
index bd2c0e3..354a6c8 100644
--- a/disk_usage_original.py
+++ b/disk_usage_fixed.py
@@ -10,7 +10,7 @@ def check_disk_usage(disk, min_absolute, min_percent):
     percent_free = 100 * du.free / du.total
     # Calculate how many free gigabytes
     gigabytes_free = du.free / 2**30
-    if gigabytes_free < min_absolute or percent_free < min_percent:
+    if percent_free < min_percent or gigabytes_free < min_absolute:
         return False
     return True
 
